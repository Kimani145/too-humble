import React from 'react';

// Web and Expo Go stub.
// The native implementation lives in AdBanner.native.tsx
// and is loaded automatically by Metro on iOS/Android EAS builds.
export function AdBanner(): React.JSX.Element | null {
  return null;
}
